import { Button } from "react-bootstrap";

export default function PNGButton() {
    return (
        <Button
            variant="outline-secondary"
            style={{ color: "blue" }}
        >
            PNG
        </Button>
    )
}